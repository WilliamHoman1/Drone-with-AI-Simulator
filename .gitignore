# Python
venv/
__pycache__/
*.pyc
.env

# Model weights (auto-downloaded by ultralytics if missing)
models/*.pt

# Unity
# Unity project is its own git repo (has its own .gitignore); keep this repo focused on the simulator
UAV-Swarm-Swim/

# ROS / external subproject folders
ros2_ws/src/ROS-TCP-Endpoint/
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/

# Mac
.DS_Store
