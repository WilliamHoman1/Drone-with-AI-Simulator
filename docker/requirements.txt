# pinned to satisfy both colcon-core (<80) and ultralytics/torch (>=77.0.3)
setuptools>=77,<80
anthropic
fastapi
uvicorn
ultralytics
opencv-python-headless
numpy
# dashboard, so the whole stack can run in one container
streamlit
plotly
requests
